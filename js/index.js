// 1 завднання

const oneString = prompt("ведіть тест");
const twoString = prompt("замок чи замок")

if (oneString === "" || twoString === ""){
    alert("Не всі поля заповнені")
} else {
    alert("Обидва поля заповнені")
}

// 1 завднання